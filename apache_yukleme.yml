---

  - hosts: all
    become: true
    tasks:

    - name: Bu gorev, apt packet indexini gunceller.
      apt:
       update_cache: yes
   
    - name: Bu gorev, apache2 yuklemesini gerceklestirir.
      apt:
       name: apache2
       state: latest

    - name: Bu gorev, apache icin php kutuphanesinin yuklemesini gerceklestirir.
      apt:
       name: libapache2-mod-php
       state: latest      
